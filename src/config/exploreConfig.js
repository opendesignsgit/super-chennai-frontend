// Allowed categories for explore discovery
export const allowedCategories = [
  "beach",
  "cafe",
  "park",
  "temple",
  "restaurant",
  "shopping"
];

// Allowed locations for explore discovery
export const allowedLocations = [
  "ecr",
  "adyar",
  "kilpauk",
  "anna-nagar",
  "velachery",
  "besant-nagar"
];

// Slug pattern validation regex
export const slugPattern = /^([a-z-]+)-places-near-([a-z-]+)$/;
